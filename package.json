{
  "name": "@simplify/root",
  "private": true,
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -pt run build"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
